
ktor:
  application:
    modules:
      - org.burgas.ApplicationKt.module
  deployment:
    port: 9000
  postgres:
    driver: org.postgresql.Driver
    url: jdbc:postgresql://postgres:5432/restaurant_care_db
    user: postgres
    password: postgres